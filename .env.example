# OpenAI (optional if using browser-stored key only)
OPENAI_API_KEY=sk-your-api-key-here

# Demo mode: canned proof outputs, no API calls. Set to true in .env.local for
# screenshots, teaching, or trying the UI without credits.
DEMO_MODE=false
